#include <iostream>
#include <vector>
#include <sstream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
	string in;
	getline(cin, in);

	stringstream ss(in);
	string token;
	vector<int> 
}
